import React from 'react';
import Nav from './Components/Nav';
import Routing from './Utils/Routing';
function App() {
  return (
    <> 
      <Nav />
      <Routing/>
    </>
  )
}

export default App
 // why not anchor tag used and why link tag used
 // what is routes and its uses and importance and drawback 
 // how link and routes are connected with each other 
 // use of navlink, its differnece from link and advantages  
 //calback function inside style inside alink and its uses and benifits( differnce types of ethods for using //isactive feature in navlink )
